"""playcricket_ble_bridge — receive Play-Cricket Scorer 'generic external
scoreboard' BLE notifications and re-expose them as a Play-Cricket HTTP API
on localhost, so the existing scoreboard24 binary can poll without internet."""
